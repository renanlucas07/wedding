<div class="container">
  <div class="row">
    <div class="offset-lg-2 col-lg-8">
      <%= render partial: 'guests/leader' %>
      <%= render partial: 'guests/rsvp_nav', locals: { active: 2 } %>

      <h2>Acompanhantes</h2>
      <p>Por favor nos conte se você trará mais alguém para o casamento. Crianças também são muito bem vindas.
      <p>&nbsp;</p>

      <% if @plus_ones.any? %>
        <%= render @plus_ones %>
      <% else %>
        <p>Nenhum acompanhante ainda.</p>
      <% end %>
      <p>
        <%= link_to 'Adicionar Acompanhante', new_guest_plus_one_path(@guest), class: 'btn btn-primary', data: { disable_with: 'Carregando...' } %>
      </p>

      <p>&nbsp;</p>
      <p class="guests-buttons">
        <%= link_to 'Continuar', confirm_guest_path(@guest), class: 'btn btn-primary' %>
      </p>
    </div>
  </div>
</div>
